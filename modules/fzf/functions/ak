##################################################
# Kill one or multiple running processes.
##################################################

local pid
pid=$(ps -ef | sed 1d | fzf --query="$1" --multi --select-1 --exit-0 | awk '{print $2}')

if [[ "x$pid" != "x" ]]; then
  echo $pid | xargs kill -${1:-9}
fi
